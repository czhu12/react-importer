export type ImporterTheme = {
  colors: {
    primary: string;
    secondary: string;
    tertiary: string;
    success: string;
    danger: string;
    warning: string;
    info: string;
  };
};

export type ThemeVariant = 'default' | 'theme-1' | 'theme-2';
